<!-- 总订单详情 -->
<view>
    <header navbar-data='{{nvabarData}}' />
    <view class="container" style="padding-top:{{occupation}}px;">
        <view class="block">
            <view class="order">
                <view class="top">
                    <text class="title">{{detail.name}}</text>
                    <text class="status">{{detail.status}}</text>
                </view>
                <view class="middle" wx:for="{{detail.goods}}" wx:key="{{index}}" wx:for-item="g">
                    <image src="{{g.goods_info.thumb_image}}" />
                    <view>
                        <view class="name">{{g.goods_info.title}}</view>
                        <view class="unit">
                            {{g.goods_info.specification}}
                            <text class="num">x{{g.num}}</text>
                        </view>
                        <view class="price">￥{{g.price}}</view>
                    </view>
                </view>
                <view class="bottom">
                    <view>
                        <text>运费</text>
                        <text>{{detail.cost_freight}}</text>
                    </view>
                    <view>
                        <text>优惠</text>
                        <text>{{detail.pmt}}</text>
                    </view>
                    <view>
                        <text>实际支付</text>
                        <text>￥{{detail.money}}</text>
                    </view>
                </view>
            </view>
        </view>
        <view class="block info">
            <view>
                订单号：{{detail.item_num}}
                <text class="button copy" bind:tap="copyText">复制</text>
            </view>
            <view>支付流水账号：{{detail.pay.pay_id}}</view>
            <view wx:if="{{detail.item_status!=0&&detail.item_status!=-1}}">支付方式：微信支付</view>
            <view wx:if="{{detail.item_status!=0&&detail.item_status!=-1}}">付款时间：{{detail.pay.createAt}}</view>
            <view wx:if="{{detail.item_status==2}}">发货时间：{{detail.deliveryTime}}</view>
        </view>
        <view wx:if="{{detail.item_status!=0}}" class="call">拨打电话</view>
        <view class="ops" wx:if="{{detail.item_status!=0}}">
            <text class="button" wx:if="{{detail.item_status==2||detail.item_status==3}}">
                物流
            </text>
            <view wx:if="{{detail.item_status==2}}" class="button">确认收货</view>
        </view>
    </view>
</view>